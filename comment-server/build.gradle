dependencies {
    implementation project(':common-component')
}

tasks.named('bootJar') {
    archiveBaseName = "comment-server"
    mainClass = "msa.comment.CommentServerApplication"
}
tasks.named('jar') { enabled = false }
tasks.register("prepareKotlinBuildScriptModel"){}
