tasks.named('bootJar') {
    archiveBaseName = "user-server"
    mainClass = "msa.user.UserServerApplication"
}
tasks.named('jar') { enabled = false }
tasks.register("prepareKotlinBuildScriptModel"){}